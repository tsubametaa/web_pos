<script lang="ts">
	interface Props {
		size?: 'sm' | 'md' | 'lg';
		class?: string;
	}

	let { size = 'md', class: className = '' }: Props = $props();

	const sizes = {
		sm: 'h-4 w-4 border-2',
		md: 'h-8 w-8 border-3',
		lg: 'h-12 w-12 border-4'
	};
</script>

<div class="flex items-center justify-center {className}">
	<div
		class="animate-spin rounded-full border-sage-200 border-t-sage-500 {sizes[size]}"
	></div>
</div>
